import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        paddingVertical: 5,
        maxHeight: 175
    },
    text: {
        color: '#FFF',
        fontSize: 12,
        fontWeight: 'bold'
    }
})