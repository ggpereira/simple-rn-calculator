import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        marginTop: 10,
        marginHorizontal: 15,
        flexDirection: 'row',
        justifyContent: 'space-around' 
    }
})